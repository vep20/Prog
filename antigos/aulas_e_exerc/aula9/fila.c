#include <stdio.h>
#define max 100

typedef struct fila_t{
   int fim;
   int membro[max]
}fila_t;


int enfileirar(*ponteiro_para_fila, membro); retorna 0 caspo a pila esteja cheia
int desenfilerar(*ponteiro_para_fila, *membro); retorna se da certo o desemfilheiramento 
int eh_cheia(*ponteiro_para_fila); retorna se a fila estiver cheia, ou seja se o fim == max
int eh_vazia(*ponteiro_para_fila); retorna vazia caso fim == 0
int ve_inicio(*ponteiro_para_fila, *membro);
//furafila

